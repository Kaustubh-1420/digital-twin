[project]
name = "smplx-measure"
version = "0.1.0"
description = "Monocular body measurement estimation using SMPL-X"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
